// 集中管理 lucide-react 图标的 re-export
// 让各组件统一从 './icons' 取图标，方便后续批量替换/审查

import {
  X,                // × ✕ 关闭
  Check,            // ✓
  AlertCircle,      // ✗
  ChevronDown,      // ▾
  ChevronRight,     // ▶（折叠状态）
  ChevronUp,        // ▲
  ArrowLeft,        // ←
  Settings,         // ⚙
  Plus,             // +（新增按钮）
  Trash2,           // 删除
  Star,             // ★（默认）
  Play,             // ▸ Resume 入口
  Square,           // ☐（未完成）
  CircleDot,        // ●
  Circle,           // ○
  Plug,             // 连接（optional）
  CornerDownRight,  // ↳
  Save,             // 保存按钮
} from 'lucide-react';

export {
  X, Check, AlertCircle, ChevronDown, ChevronRight, ChevronUp, ArrowLeft,
  Settings, Plus, Trash2, Star, Play, Square, CircleDot, Circle, Plug,
  CornerDownRight, Save,
};